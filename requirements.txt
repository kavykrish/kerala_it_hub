# Kerala IT Hub - Backend (FastAPI + MCP + RAG) dependencies
#
# Embeddings run on fastembed (ONNX runtime), not sentence-transformers/
# torch. Torch's own import overhead alone (before it does any work) was
# enough to blow past a 512MB free-tier memory limit -- fastembed uses the
# same all-MiniLM-L6-v2 model but runs on the much lighter onnxruntime.

fastapi==0.141.1
uvicorn[standard]==0.53.0
groq==1.7.0
mcp==2.2.0
mcp-types==2.2.0
pydantic==2.13.5
python-dotenv==1.2.3
requests==2.34.2
fastembed==0.8.0
trafilatura==2.2.0
ddgs==9.16.0
numpy==2.5.3
